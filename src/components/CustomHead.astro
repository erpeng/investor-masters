---
const { head } = Astro.locals.starlightRoute;
---

{head.map(({ tag: Tag, attrs, content }) => <Tag {...attrs} set:html={content} />)}
<script src={`${import.meta.env.BASE_URL}/sidebar-toggles.js`}></script>
